# java.util.logging (JUL) config for platform tests.
# Since the IntelliJ Platform switched its logging to JUL, the old idea/log4j.xml is ignored.
# Silence everything: no handlers, root level OFF.
handlers=
.level=OFF
